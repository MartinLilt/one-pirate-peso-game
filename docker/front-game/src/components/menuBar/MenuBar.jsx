import { StyledMenuBar, StyledButton } from "./menuBar.styled";

const MenuBar = () => {
  return (
    <StyledMenuBar>
      <StyledButton type="button"></StyledButton>
      <StyledButton type="button"></StyledButton>
      <StyledButton type="button"></StyledButton>
      <StyledButton type="button"></StyledButton>
      <StyledButton type="button"></StyledButton>
      <StyledButton type="button"></StyledButton>
    </StyledMenuBar>
  );
};

export default MenuBar;
